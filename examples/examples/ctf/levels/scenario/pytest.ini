# Target-agnostic scenario level.
#
# rootdir is this directory, so it does not inherit parent conftests. The plugins
# package lives two levels up (examples/examples/ctf), added to the import path.
[pytest]
pythonpath = ../..
addopts = -ra
