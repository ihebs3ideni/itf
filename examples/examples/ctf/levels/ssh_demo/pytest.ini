# SSH-over-token demo level (fully mocked). Shows a lifecycle dependency (token
# deployment) sequenced by the graph, and one SSH plugin reused across targets.
[pytest]
pythonpath = ../..
addopts = -ra
